{{ form_start(form, {'attr': { 'class': 'embedded'}}) }}
{% if label is defined %}
<label class="oocss-label">{{ label }}</label>
{% endif %}
{{ form_row(form.name, {'label': 'nom', 'widget_attr': {'class': 'oocss-full-container-width'}, 'help': 'choisir un nom'}) }}
{{ form_end(form) }}